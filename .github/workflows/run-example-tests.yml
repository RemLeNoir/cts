name: tests-ejemplos

on:
  push:
    branches: [main]
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install pytest pyyaml jsonschema

      - name: Regenerar biblioteca/biblioteca.json (sanity check)
        run: python scripts/build_biblioteca.py

      - name: Ejecutar tests de ejemplos (capa formal)
        run: python scripts/run_example_tests.py

      - name: Ejecutar pytest (incluye coherencia md vs json)
        run: pytest -q
